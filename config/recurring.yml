# Solid Queue is not running in production (see config/puma.rb and
# config/environments/production.rb — siscartd's PostgreSQL is too old for
# it), so this file's schedule is never read by anything. The one recurring
# job that used to live here, EnvioAutomaticoAtosJob (every minute), now runs
# from config/initializers/envio_automatico_scheduler.rb instead.
